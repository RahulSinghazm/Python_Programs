def cricket(m):
    r=m
    point=0
    run=0
    m=int(input("start run point"))
    if run<=100:
        if run<80:
            point=run/2
            if run==50:
                point=point+5
                if striekrate>80:
                    if striekrate <100:
                        point<=run*2
                        if striekrate>100:
                            point=run*4
                            m2=int(input("run=point:"))
                            
